apiVersion: v1
kind: Service
metadata:
  name: case-platform-shell
spec:
  type: ClusterIP
  selector:
    app: case-platform-shell
  ports:
    - name: http
      port: 80
      targetPort: http