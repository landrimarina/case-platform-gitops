apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: case-platform-postgresql
spec:
  storage:
    storageClass: local-path

  resources:
    requests:
      cpu: 250m
      memory: 512Mi
    limits:
      cpu: "1"
      memory: 1Gi